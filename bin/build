#!/bin/sh -eu

__script_name="$0"

while [ -L "$__script_name" ]; do
  __script_name="$(readlink "$__script_name")"
done

cd "$(dirname "$__script_name")/.."

unset -v '__script_name'

[ "$PWD" != "$(pwd -P)" ] && export PWD="$(pwd)"

# TODO
